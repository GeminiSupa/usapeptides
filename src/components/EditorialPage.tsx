import Link from 'next/link';

export default function EditorialPage({ eyebrow, title, intro, sections, links = [] }: { eyebrow: string; title: string; intro: string; sections: { title: string; body: string }[]; links?: { href: string; label: string }[] }) {
  return <div className="shell space-y-12 py-12"><header className="max-w-3xl border-b border-brand-border pb-8"><p className="eyebrow mb-3">{eyebrow}</p><h1 className="page-title">{title}</h1><p className="mt-4 text-sm leading-relaxed text-brand-body">{intro}</p></header><div className="grid gap-5 md:grid-cols-2">{sections.map((s) => <section key={s.title} className="border border-brand-border bg-brand-card p-6"><h2 className="font-display text-sm font-extrabold uppercase tracking-wide text-brand-heading">{s.title}</h2><p className="mt-3 text-sm leading-relaxed text-brand-textMuted">{s.body}</p></section>)}</div>{links.length > 0 && <div className="flex flex-wrap gap-3">{links.map((l) => <Link key={l.href} href={l.href} className="btn-ghost">{l.label}</Link>)}</div>}<p className="border-t border-brand-border pt-6 text-xs leading-relaxed text-brand-textMuted">Laboratory research use only. Not for human or veterinary use.</p></div>;
}
